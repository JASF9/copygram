extends layout

block content
  h1= title

  ul
    each user in user_list
      li
        a(href=user.url) #{user.nickname}
        |  (Likes #{user.likes}) - Followers #{user.followers})

    else
      li There are no users